# Optional, trusted shell configuration. Copy to .runtime.env (gitignored).
# DSH_REMOTE_NODE_BIN=/absolute/path/to/node/bin
# DSH_REMOTE_ANDROID_TOOLCHAIN=/absolute/path/to/android-toolchain
# Or configure JAVA_HOME, ANDROID_HOME and put gradle on PATH.
# DSH_REMOTE_GRADLE_OFFLINE=1  # Only after all Android dependencies are cached.
